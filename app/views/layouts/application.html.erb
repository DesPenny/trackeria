<!DOCTYPE html>
<html>
<head>
  <title>WebTracker</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="contain-to-grid sticky">
<nav class="top-bar">
  <ul class="title-area">
    <!-- Title Area -->
    <li class="name">
      <h1><a href="/">WebTracker (beta)</a></h1>
    </li>
    <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Left Nav Section -->
    <ul class="left">   
         <% if user_signed_in? %>
    

    <li class="has-form">
    <%= form_tag domainurls_path, :method => 'get', :id=>"domainurls_search" do %>
    <%= hidden_field_tag :direction, params[:direction] %>
    <%= hidden_field_tag :sort, params[:sort] %>
    <%= text_field_tag :search, params[:search] %></li>
    <li class="has-form"><%= submit_tag "Search", :class=>"button secondary round" %></li>
    <% end %>
    <% end %>
    </ul>
    <ul class="right">
        <% if user_signed_in? %>
        <li class="active"><%= link_to "Help", edit_user_registration_path %></li>
        <li class="divider"></li>
        <% if current_page?(root_path) %>
      <li class="active"><%= link_to (@domaincount), edit_user_registration_path %></li>
        <li class="divider"></li>
        <% end %>
        <li class="active"><%= link_to current_user.email, edit_user_registration_path %></li>
        <li class="divider"></li>
        <li class="active"><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
       
     
        
        <% else %>
        <li class="active"><%= link_to "Sign up", new_user_registration_path %></li>
        <li class="divider"></li>
        <li class="active"><%= link_to "Login", new_user_session_path %></li>
    <% end %>
    </ul>
  </section>
</nav>
</div>
<br /><br />
<% if notice!=nil %>
<p />
<div class="row">
    <div class="small-4 small-centered columns">
        <center><p class="notice"><%= notice %>
        </center>
    </div>
</div>
<% end %>
<% if alert!=nil %>
<p />
<div class="row">
    <div class="small-4 small-centered columns">
        <center>
        <p class="alert"><%= alert %></center>
    </div>
</div>
<% end %>
<%= yield %>

</body>
</html>
