<%= form_for(@domainurl) do |f| %>
  <% if @domainurl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@domainurl.errors.count, "error") %> prohibited this domainurl from being saved:</h2>

      <ul>
      <% @domainurl.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  
  <div class="field">
    <%= f.label :keyword %><br />
    <%= f.text_field :keyword %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>
  
  <div class="button">
    <%= f.submit %>
  </div>
<% end %>
